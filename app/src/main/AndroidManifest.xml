<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rava.ventoapp.version2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.rava.ventoapp.version2.UpdateActivity"
            android:label="Update/Hapus Barang"
            android:parentActivityName="com.rava.ventoapp.version2.MainActivity"/>
        <activity
            android:name="com.rava.ventoapp.version2.AddActivity"
            android:label="Tambah Barang"
            android:parentActivityName="com.rava.ventoapp.version2.MainActivity" />
        <activity android:name="com.rava.ventoapp.version2.HistoryActivity"
            android:label="History Barang"
            android:parentActivityName="com.rava.ventoapp.version2.MainActivity"/>
        <activity
            android:name="com.rava.ventoapp.version2.MainActivity"
            android:label="Vento by PINS Jabar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>